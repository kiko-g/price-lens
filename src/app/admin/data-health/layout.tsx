import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Saúde dos Dados | Admin",
}

export default function DataHealthLayout({ children }: { children: React.ReactNode }) {
  return children
}
